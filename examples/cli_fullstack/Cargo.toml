[package]
name = "cli_fullstack"
version = "0.1.0"
edition = "2024"
default-run = "cli_fullstack"

[dependencies]
axum = "0.8"
che-orm = { path = "../../../che-orm/crates/che-orm" }
che-rest = { path = "../.." }
serde_json = "1"
tokio = { version = "1", features = ["macros", "net", "rt-multi-thread", "sync"] }
